# Pastenator

Safe GitHub package for the Pastenator PHP application.

## Setup

Run `composer install`, then create a writable `storage/` directory. The production SQLite database, paste storage, vendor folder, logs, and backups are intentionally not included.
